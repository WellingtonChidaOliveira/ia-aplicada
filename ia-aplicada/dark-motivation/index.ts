import { buildServer } from "./src/server/fastify.ts";

const server = buildServer();

server.listen({ port: 3000 }, (err, address) => {
  if (err) {
    console.error(err);
    process.exit(1);
  }
  console.log(`Server listening on ${address}`);
});
